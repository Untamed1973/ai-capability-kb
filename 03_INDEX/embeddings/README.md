# Embeddings — [PLACEHOLDER]

> **Nog niet werkend.** Semantisch zoeken via embeddings is een latere toevoeging. Er is
> nu geen executor-/index-laag die embeddings genereert of doorzoekt. Dit is bewust een
> placeholder, conform de regel: geen onderdelen die doen alsof ze werken.

Wanneer dit wél gebouwd wordt, hoort hier: hoe embeddings gegenereerd worden, waar ze
staan, en hoe de router ze gebruikt voor semantisch matchen bovenop tag-/tekstzoek.
